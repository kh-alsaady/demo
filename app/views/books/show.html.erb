<%= link_to "Edit", edit_book_path(@book), class: 'btn btn-success btn-xs' %>
<%= link_to "Destroy", book_path(@book), class: 'btn btn-danger btn-xs', method: :delete, data: {confirm: "Are you sure ?"} %>
<br /><br />
<%= render @book %>