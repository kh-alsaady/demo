<%= link_to "Edit", edit_course_path(@course), class: 'btn btn-success btn-xs' %>
<%= link_to "Destroy", course_path(@course), class: 'btn btn-danger btn-xs', method: :delete, data: {confirm: "Are you sure ?"} %>
<br /><br />
<%= render @course %>