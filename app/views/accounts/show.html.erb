
<%= link_to "Edit", edit_account_path(@account), class: 'btn btn-success btn-xs' %>
<%= link_to "Destroy", account_path(@account), class: 'btn btn-danger btn-xs', method: :delete, data: {confirm: "Are you sure ?"} %>
<br /><br />
<%= render @account %>